export interface IResDetailOverlay {
  id: string;
  created_date: Date;
  text?: string | null;
  type?: string | null;
  background_color: string;
  text_color: string;
  highlight_color: string;
}
